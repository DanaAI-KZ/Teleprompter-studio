# 🎬 Teleprompter Studio

**Teleprompter Studio** — профессиональный телесуфлёр для видеопродакшна, телевидения и стриминга с двухоконным режимом, плавной прокруткой и зеркальным отображением.  
Проект разработан как высокопроизводительное веб-приложение, упакованное в **Electron (Portable)**.

---

## ✨ Основные возможности

### 🖥 Двухоконный режим
* **Главное окно управления:** Полный контроль настроек и текста.
* **Окно диктора (Prompter Mode):** Чистый интерфейс без лишних элементов.

### 🔄 Зеркальный режим
* Специальный режим для работы с физическими телесуфлёрными зеркалами (делителями луча).
* Активен только в окне диктора, не мешая оператору.

### 🎚 Плавная прокрутка
* **GPU-ускорение:** Использование `translate3d` для максимальной плавности.
* **Сглаживание:** Алгоритмы инерции (easing) для комфортного чтения.

### ⌨️ Горячие клавиши
| Клавиша | Действие |
| :--- | :--- |
| **Space** | Старт / Пауза |
| **↑ / ↓** | Скорость прокрутки |
| **← / →** | Размер шрифта (только в паузе) |
| **PageUp / PageDown** | Ширина текста (только в паузе) |
| **Wheel (Колесо)** | Ручная прокрутка (только в паузе) |
| **Home** | Мгновенный переход в начало текста |

---

## 🧠 Технологический стек
* **Engine:** Vanilla JavaScript (без тяжелых фреймворков).
* **Graphics:** CSS3 (GPU-ускорение, `will-change`, `translate3d`).
* **Sync:** BroadcastChannel API (нулевая задержка между окнами).
* **Runtime:** Electron (упаковка в Desktop-приложение).
* **Persistence:** LocalStorage (автосохранение текста и настроек).

---

## 📁 Структура проекта
```text
teleprompter-studio/
 ├── main.js         # Точка входа Electron
 ├── index.html      # Интерфейс и логика промптера
 ├── logo.png        # Логотип и иконка приложения
 ├── package.json    # Конфигурация проекта и сборки
 └── README.md       # Документация
🖥 Режим диктора (Prompter Mode)
Окно диктора открывается кнопкой «ОКНО ДИКТОРА».

Интерфейс управления полностью скрыт.

Текст и прогресс чтения синхронизируются мгновенно.

Приложение сохраняет геометрию (размер и положение) окна диктора.

📦 Запуск и сборка
Установка зависимостей
Bash

npm install
Запуск в режиме разработки
Bash

npm start
Сборка Portable-версии (Windows)
Bash

npm run build
Результат: Файл dist/Teleprompter Studio.exe, готовый к работе без установки.

👤 Автор
Nurlan Akkainanov CEO, DANA SYSTEMS Telegram: @NurlanDS

Website: www.dana-systems.com

🏢 О компании
DANA SYSTEMS — Systems That Think. Разработка ПО • Криптотехнологии • Инфраструктурные решения.

© 2026 DANA SYSTEMS. Все права защищены.